<% content_for :title, "แก้ไขงาน" %>

<div class="max-w-2xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-2xl">
  <h1 class="text-2xl font-bold text-indigo-700 mb-6">📝 แก้ไขงาน</h1>

  <%= render "form", task: @task %>

  <div class="mt-6 flex flex-col sm:flex-row justify-between gap-4">
    <%= link_to "ดูงานนี้", @task, class: "btn btn-info btn-sm" %>
    <%= link_to "← กลับหน้ารายการงาน", tasks_path, class: "btn btn-outline btn-sm" %>
  </div>
</div>

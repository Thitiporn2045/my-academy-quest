<% content_for :title, "แก้ไขงาน" %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <div class="bg-base-100 shadow-xl rounded-2xl p-6">
    <h1 class="text-3xl font-bold text-center text-primary mb-6">✏️ แก้ไขงาน</h1>

    <%= render "form", task: @task %>

    <div class="mt-6 flex flex-col sm:flex-row justify-between items-center gap-4">
      <%= link_to "🔍 ดูงานนี้", @task, class: "btn btn-info" %>
      <%= link_to "← กลับหน้ารายการงาน", tasks_path, class: "btn btn-outline" %>
    </div>
  </div>
</div>
